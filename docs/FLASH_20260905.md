# 2026-09-05 USB 固件更新记录

- 用户明确要求烧录。设备通过本机串口连接，ESP32-S3 rev 0.2，16 MB flash；设备唯一标识已从公开记录中移除。
- 先读取 `0x8000` 起 `0xA000` 字节到 `build/preflash_20260905_metadata.bin`，包括分区表、完整 NVS、OTA 选择区及 PHY。
- 设备分区表与构建分区表一致；OTA 序号 1 选择 `ota_0`。
- 仅在 `0x20000` 写入 `build/obd_brz_gauge.bin`，2046544 字节。没有擦除整片 flash，没有写入 NVS、分区表、bootloader、OTA 选择区或 bootmedia。
- esptool 返回 `Hash of data verified`。
- 保持 bootloader 状态，再读同一元数据范围到 `build/postflash_20260905_metadata.bin`；两个文件逐字节完全一致。此比较在第一次启动新固件之前执行，证明烧录本身未改变历史和配置。
- 复位后读取 18 秒启动日志。启动地址为 `0x20000`，ELF SHA256 前缀 `275d1ddc8` 与本地 `build/obd_brz_gauge.elf` 一致；完整哈希 `275d1ddc8b2c941488c2e35812099b22be9113eea2bbb2a8f7cb30640c27659a`。
- 显示驱动和触摸初始化完成，BLE 广播 `SkyGauge-<设备后缀>` 启动；没有观察到该窗口内的崩溃或重启循环。
- 启动日志另有未连接 RS485 设备超时、bootmedia manifest 加载失败、`Failed to create OTA validity task` 警告。此次没有更改这些模块；当前 OTA 分区原有状态已为 VALID。不能据本次 USB 烧录成功认为未来 OTA 流程也已通过验证，OTA 更新前应检查该任务警告。
- 本次没有进行实车 OBD 和鸿蒙手机端 BLE 联调。

手机包 `android_app/BRZ-Garage-v2.0.1-debug.apk` 已编译，applicationId `com.brz.gauge.trips`，versionCode 3。证书 SHA256 与此前安装包相同：`08bab8a9c07f8e5b0efcfb39f6c9ee56bea08021c7e5b231be7efff0aaa43f50`。

## 延迟授时稳健性固件更新（12:00）

- 为当前行程加入单调时钟锚点：即使启动车辆时未获得手机时间，只要本次设备持续通电并在之后成功同步一次，即可回推行程开始时间；同步发生在停车之后时，会扣除最后一次发动机采样后的等待时间。
- 行程驾驶时长改为依据有效发动机运行采样累积，不再依赖油耗 PID 是否可用；时间边界同时保证不会短于已累计驾驶时长。
- 主机侧回归测试共 18 项，全部通过。
- 烧录前读取 `0x8000` 起 `0xA000` 字节到 `build/preflash_delayed_time_20260905_1200_metadata.bin`。
- 仅在 `ota_0` 的 `0x20000` 写入 `build/obd_brz_gauge.bin`，2047488 字节；esptool 返回 `Hash of data verified`。
- 新固件 BIN SHA256：`316ba778a8090d98c9b4a37e3139372d3705637c89bc6c9eb3e95d743ed9a070`。
- 首次启动前将同一区域读回到 `build/postflash_delayed_time_20260905_1200_metadata.bin`；与烧录前备份逐字节一致（40960 字节），因此分区表、NVS 行程历史/设置、OTA 选择区与 PHY 未被本次写入改变。
- 复位后从 `0x20000` 正常启动；设备报告 ELF SHA256 前缀 `cbfb0fb0a`，与本地完整哈希 `cbfb0fb0afc52e7a724e5cc6ae027b3287f73b32894ae301b5924162f5860706` 一致。显示、触摸、BLE OTA 服务均完成初始化，BLE 正常广播 `SkyGauge-<设备后缀>`，18 秒观察窗口内未出现崩溃或重启循环。
- 仍存在此前已知的未接 RS485 传感器超时、缺少 bootmedia manifest，以及 `Failed to create OTA validity task` 警告；与本次时间记录改动无关。
- 限制：若设备在整个驾驶阶段从未获得绝对时间，随后完全断电，ESP32 的本次单调时钟锚点也随之丢失，下一次开机无法可靠恢复上一段行程的真实日期和开始时间。

## ZD8 默认车型与 215/40R18 更新（15:35）

- 主机侧 19 项回归测试通过，固件大小 2047552 字节。
- 烧录前备份 `0x8000` 起 `0xA000` 到 `build/preflash_zd8_fuel_20260905_153542_metadata.bin`；仅将应用写入 `ota_0` 的 `0x20000`。
- esptool 返回 `Hash of data verified`。首次启动前读回 `build/postflash_zd8_fuel_20260905_153542_metadata.bin`，与备份逐字节一致（40960 字节）。
- 固件 BIN SHA256：`614e98bcc51231ce32035c5444fb70c34daf85a241344f9a8baa4526f067782c`。
- 启动时完成配置 v8 到 v9 的一次性迁移，日志确认 `profile=4(ZD8)`；ZD8 推算轮胎半径为 0.315 m，对应用户车辆 `215/40R18`。
- 启动 ELF SHA256 前缀 `02621e9c3` 与本地完整哈希 `02621e9c324fc980ac785e273793a9310565cba89c3537754fead29b6afac0cb` 一致。显示、触摸和 BLE 广播启动正常，20 秒内没有崩溃或重启。
- 台架启动期间未连接车辆 ELM327，未验证实车 `01 10`/`01 5E` 油耗数据。

## 短时断电行程合并修复（2026-09-12）

- 修复合并判定错用“手机延迟授时时刻”代替“本次实际驾驶开始时刻”的问题，并保留多次额外复位之间未决的待合并行程。
- 9 项相关主机回归测试和完整 ESP-IDF 构建通过；应用镜像 2,090,976 字节，最小应用分区仍有 34% 余量。
- 仅在 `ota_0` 的 `0x20000` 写入 `build/obd_brz_gauge.bin`；esptool 返回 `Hash of data verified`。BIN SHA256：`f89de40138426acf96330eaddf91752e40c3413b6205a6639b0cadc855a6a49b`。
- 烧录前后各读取 `0x8000` 起 `0xA000` 字节元数据。差异只出现在 NVS 分区 `0x9000–0xEFFF`；分区表、OTA 选择区和 PHY 区完全一致。原因是首次读取结束后的自动复位让旧固件短暂启动并正常更新 NVS；烧录擦写范围仅为 `0x20000–0x21EFFF`，未覆盖 NVS。两份镜像均已保留。
- 已硬复位退出下载模式。为避免原生 USB 串口再次触发复位，本次不打开串口监视器。
